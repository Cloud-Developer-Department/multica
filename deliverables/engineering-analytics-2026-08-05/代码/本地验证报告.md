# 本地验证报告：研发效能分析平台后端实现（CLO-239）

> 验证时间：2026-08-06（含阶段10 Review 问题修复轮）
> 环境：Windows，Go 1.25.12，PostgreSQL 15.10（本地部署 `C:\multica-deploy\multica`，分支 `Equipment_Department_Exploration`）

## 1. 构建基线

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| 分支基线 | `git branch --show-current` | `Equipment_Department_Exploration` ✅ |
| 全量编译 | `go build ./...` | ✅ 通过 |
| 服务端二进制 | `go build ./cmd/server` | ✅ 通过 |
| 静态检查 | `go vet ./internal/handler/` | ✅ 通过 |
| 迁移应用 | `schema_migrations` 含 `254_analytics_dashboard` + `255_analytics_identity_result_nullable` | ✅ 已应用 |
| 新增数据表 | `deployment_event` / `identity_import` / `repo_quality_snapshot` / `vcs_author_mapping` / `member.department` | ✅ 已建 |
| 路由注册 | `GET /api/analytics/activity/summary` 返回 `401 {"error":"missing authorization"}`（路由存在，非 404） | ✅ |

## 2. 单元测试（`analytics_dashboard_test.go`，20 项全部通过）

执行 `go test ./internal/handler/ -run "TestAnalytics" -count=1 -v`：

| 测试 | 覆盖场景 | 结果 |
| --- | --- | --- |
| `TestAnalyticsUnauthorized` | 19 个接口未登录 → 全部 401 | ✅ PASS |
| `TestAnalyticsActivitySummary` | A1 汇总数学（成员/活跃/人均 Issue/窗口） | ✅ PASS |
| `TestAnalyticsActivityHeatmap` | A2 三口径 metric 回显 + day/hour/value 结构 + max_value | ✅ PASS |
| `TestAnalyticsAdoption` | A4 汇总（指派占比/覆盖率）+ A5 趋势点结构 | ✅ PASS |
| `TestAnalyticsAgentFunnelNoVCS` | B1 无 VCS → `merged_count=null`（引导态）；assign/execute 计数 | ✅ PASS |
| `TestAnalyticsAgentPerformance` | B2 终态计数/成功率/时长 P50/P95 + B3 排行 | ✅ PASS |
| `TestAnalyticsSkillsOverview` | B4 技能总量/新增/积累/复用 | ✅ PASS |
| `TestAnalyticsGitGuideStates` | G1–G4 无 Git 数据源 → `200 + source_status.ready=false + reason` | ✅ PASS |
| `TestAnalyticsDoraGuideStates` | D1（deploy/merged）D2 无部署数据 → 引导态 | ✅ PASS |
| `TestAnalyticsIdentityGuideStates` | L1/L2 无身份数据 → 引导态 | ✅ PASS |
| `TestAnalyticsDepartmentFilterUnconfigured` | 未配置部门时传 `department_id` → 400（E18） | ✅ PASS |
| `TestAnalyticsProjectIDMalformed` | 非法 `project_id` → 400 | ✅ PASS |
| `TestAnalyticsCollaborationBlockers` | B5 协作汇总 + B6 Blocker 明细（activity_log 引导） | ✅ PASS |
| `TestAnalyticsLimitClamping` | `limit`/`days` 钳制与回退 | ✅ PASS |
| `TestAnalyticsQualityNullableSnapshot` | G2 快照 `coverage`/`duplication_rate` 为 NULL → 200 + null（**P1-03**） | ✅ PASS |
| `TestAnalyticsDeploymentsTrendMttr` | D2 `trend[].mttr_seconds` 按周填充 + `updated_at`（**P1-02 / P3-02**） | ✅ PASS |
| `TestAnalyticsLeadTimeDeployUpdatedAt` | D1 deploy `source_status.updated_at` 填充（**P3-02**） | ✅ PASS |
| `TestAnalyticsIdentityResultNullable` | L1 `recent_events[].result` 允许 null（迁移 255，**P2-04**） | ✅ PASS |
| `TestAnalyticsMergedCountFiltered` | B1 有 VCS + merged PR → `merged_count` 数值（**P2-01**） | ✅ PASS |
| `TestAnalyticsBlockerNPlusOne` | B6 批量 resolution 查询，blocker 状态/响应时长正确（**P2-02**） | ✅ PASS |

## 3. 阶段10 Review 问题修复记录（后端范围）

Review（CLO-256，结论 `need_modify`）指出的后端项已在本轮修复，前端项（P0-01 列表解析、P1-01、P1-05）由 CLO-240 修复并提交 `8f6da199`：

| # | 问题 | 修复 |
| --- | --- | --- |
| P1-03 | G2 快照 `coverage`/`duplication_rate` 可空，`::float8` 扫 NULL 会 500 | SQL 改选原始可空列（`pgtype.Numeric`），handler `numericToPtr` 空值返回 `null`；汇总均值跳过 NULL 字段 |
| P1-02 | D2 `trend[].mttr_seconds` 恒 null | 新增 `ListAnalyticsDeploymentTrendMttr` 按周 P50 恢复时长 |
| P2-02 | B6/协作汇总 blocker resolution N+1 查询 | 新增 `ListAnalyticsBlockerResolutions` 批量查询，handler 一次取回 |
| P2-01 | B1 `merged_count` 不随 `department_id`/`project_id` 过滤 | `CountAnalyticsMergedIssues` 增加部门/项目过滤（关联 issue） |
| P3-02 | D1 deploy / D2 `source_status.updated_at` 恒 nil | 新增 `GetAnalyticsDeploymentLastSync` 取最近部署 `finished_at` |
| P2-04 | L1 `recent_events[].result` 恒非空，契约允许 null | 迁移 `255_analytics_identity_result_nullable` 放开 NOT NULL，handler 透传 |

**不阻塞项（记录留档，不擅自改口径）**：
- P1-04（G3 `active_commits` 恒 0）：本地 VCS 阶段未接入提交数据，属二期数据源接入事项，不阻塞契约。
- P2-03（L2 `name`=department_id）：无部门名数据源，过渡语义已在前端按 id 展示。
- P2-05 / P2-06（A2 issue_volume 人均口径、A5 按日覆盖口径）：口径调整需 Leader 确认（PRD §10），本轮不改。
- P3-01 / P3-03：`project_id` 不存在性校验、G1 `commit_count` 代理口径，已文档注明。

## 4. 契约对齐核查（API-CLO-228 v2.0）

| 契约要点 | 实现 | 验证 |
| --- | --- | --- |
| §0.2 公共参数 `days`/`tz`/`project_id`/`department_id` | `resolveAnalyticsScope` 统一解析，非法回退/400 | ✅ |
| §0.5 空值语义（null / [] / 0 逐字段） | 各响应结构逐字段对齐（接口文档 §各接口） | ✅ |
| §0.7 `source_status`（G/D/L 必含） | G1–G4 / D1–D2 / L1–L2 顶层恒返回；未接入 `ready=false+reason` | ✅ 单测覆盖 |
| 未接入数据源 = 正常态 200，不 404/5xx | 引导态分支 `writeJSON(200, …)` | ✅ 单测覆盖 |
| B1 `merged_count`：null=VCS 未接入 | `CountAnalyticsVCSConnections` 门控 | ✅ 单测覆盖 |
| ELOC 人/机分组（G1 `group_by`） | 成员/Agent/未归属三态归属，总量双写 | ✅ 代码审查 |
| 交付周期/合并周期秒→天换算 | 接口输出秒，换算留给前端 | ✅ 文档约定 |
| MTTM P50/P95 | `percentileDurations` 线性插值 | ✅ 代码审查 |
| `department_id` 仅 Tab1/Tab2/L1；未接入 → 400 | `allowDepartment` 参数 + E18 分支 | ✅ 单测覆盖 |
| `project_id` 格式非法/不存在 → 400 | `parseProjectIDParam` | ✅ 单测覆盖 |

## 5. 数据表与查询

- 新增 4 表 + 1 字段（迁移 254）：`member.department`、`repo_quality_snapshot`、`deployment_event`、`vcs_author_mapping`、`identity_import`，均带 `workspace_id` 归属、无外键（与 VCS 表约定一致，应用层只读不删）。
- 迁移 255：`identity_import.result` 放开 NOT NULL（契约允许 offboard 事件 result=null）。
- `analytics.sql` 47 条查询全部经 sqlc 生成（`analytics.sql.go`），编译通过；仅该文件重新生成，版本头与仓库 sqlc v1.28.0 对齐（避免全仓生成文件版本号 churn）。
- 指标主体复用既有表：`issue` / `agent_task_queue` / `comment` / `activity_log` / `skill` / `agent_skill` / `member` / `user` / `vcs_pull_request` / `issue_vcs_pull_request`，与 `可行性校验.md` 一致。

## 6. 已知事项

- 全量 handler 测试包中 `TestShortTaskIDMatchesDaemon`、`TestDashboardFailuresByAgentUsesExactWindow`、`TestParseSkillArchive_RejectsUnsafeSkillMdPath` 在**基线分支同样失败**（本地共享测试库数据/环境相关，与本次改动无关，已通过移除本测试文件对照确认；CLO-212 验证报告已记录同类基线失败）。
- G3 `active_commits` 当前返回 `0`：本地 VCS 阶段未接入提交数据（PR 数据可用），按 `source_status.ready` 归入正常态而非引导态；待 GitCode 线上数据接入后点亮。
- G1 ELOC 当前用 PR `additions+deletions` 作代理当量（可行性 §7.3 行数兜底先行），AST 工具链选型后替换口径，不影响接口契约。
- 演示数据未造：按父需求 CLO-227 策略「先做功能，最后统一造演示数据」，归入 DevOps 阶段（阶段12）统一造数。

## 7. 结论

后端 19 个接口实现完成，口径与 `设计/API契约-研发效能分析平台.md` v2.0 / `设计/数据口径说明.md` v2.0 逐字段对齐；编译 / 静态检查 / 单元测试（20 项）通过；阶段10 Review 指出的后端 P1/P2/P3 项已修复并单测覆盖；未接入数据源正确返回引导态契约（`200 + source_status.ready=false + reason`），已接入数据可点亮四看板；权限（401/403/400）与异常捕获齐全。前端项由 CLO-240 修复（`8f6da199`）。可进入阶段10 复审及阶段11 测试。
