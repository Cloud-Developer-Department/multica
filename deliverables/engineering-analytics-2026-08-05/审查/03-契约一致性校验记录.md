# 契约一致性校验记录 —— 阶段10 Review（研发效能分析平台）

> 逐接口核对：字段、类型、空值语义（null/[]/缺省）、`source_status.ready=false` 引导态行为。
> 基准：《API契约-研发效能分析平台.md》v2.0、《数据口径说明.md》v2.0、《PRD》§7。
> 结论标记：✅一致 / ⚠️有偏差（详见修复建议清单）。

---

## §0 通用约定

| 约定 | 校验结果 | 说明 |
| --- | --- | --- |
| 0.1 认证（工作区成员中间件） | ✅ | `/api/analytics` 注册在 `RequireWorkspaceMember` 组内（router.go:1080 起），未登录 401 单测覆盖 |
| 0.2 `days` 1-365 默认30 非法回退 | ✅ | `parseAnalyticsDays` 回退 30 |
| 0.2 `tz` IANA 非法回退 UTC/用户时区 | ✅ | `resolveViewingTZ` + `LoadLocation` 回退 |
| 0.2 `project_id` 格式非法→400 | ✅ | `parseProjectIDParam`（格式校验）；「不存在→400」未实现（P3-01） |
| 0.2 `department_id` 未接入→400（E18） | ✅ | `resolveAnalyticsScope` allowDepartment + `CountAnalyticsMembersWithDepartment==0`→400，单测覆盖 |
| 0.3 响应无包裹层、snake_case | ✅ | 业务对象直接 JSON |
| 0.4 统一错误 `{"error": ...}` | ✅ | `writeError` |
| 0.5 空值语义 null/[]/0 | ✅ | 各接口逐字段核验通过（见下） |
| 0.6 比率 0-1、时长秒、日期 YYYY-MM-DD | ✅ | 后端秒输出，前端换算 |
| 0.7 `source_status` G/D/L 恒返回 | ✅ | G1-G4/D1-D2/L1-L2 全部顶层返回；未接入 ready=false+reason+200 |

---

## Tab1 活跃度与渗透（A1–A5）

| 接口 | 字段/类型/空值 | source/引导态 | 结果 |
| --- | --- | --- | --- |
| A1 activity/summary | window/total_members/active_members/active_days_avg/dau/mau/dau_mau_ratio/active_user_ratio/total_issues/per_capita_issue_volume 全齐；null=分母0 | 无 | ✅ |
| A2 activity/heatmap | metric 回显；days 含 `[{date,hours:[{hour,value}]}]` 无活跃整日省略；max_value | 无 | ⚠️ P2-05（issue_volume 非人均）；value 恒非 null 与契约 int|null 表述差异（P3-04，可接受） |
| A3 activity/top-members | items[] 含 member_id/name/active_days/issue_count/last_active_at；[]=无数据 | 无 | ⚠️ **P0-01**：后端 `{items:[...]}` 与前端裸数组解析不匹配，前端恒空 |
| A4 adoption/summary | total_issues/agent_assigned_issues/assignment_ratio/agent_covered_issues/coverage_ratio；null=分母0 | 无 | ✅ |
| A5 adoption/trend | points[] 含 date/total_issues/assignment_ratio/coverage_ratio | 无 | ⚠️ **P0-01**（`{points:[...]}` 前端解析错）+ P2-06（按日覆盖口径） |

## Tab2 Agent 效能（B1–B6）

| 接口 | 字段/类型/空值 | source/引导态 | 结果 |
| --- | --- | --- | --- |
| B1 agents/funnel | assign_count/issue_assigned_count/execute_count/execute_ratio；merged_count/merged_ratio null=VCS未接入 | VCS 门控 | ✅（P2-01 merged 不随 dept/project 过滤） |
| B2 agents/performance | terminal/completed/failed/success_rate/avg/p50/p95/failure_classes | 无 | ✅ |
| B3 agents/top | items[] agent_id/name/completed/failed/success_rate/avg_duration | 无 | ⚠️ **P0-01**（前端解析错）+ P1-01（前端无视图消费，死代码） |
| B4 skills/overview | total_skills/new_skills/accumulation/top_reused | 无 | ✅ |
| B5 collaboration/summary | collab_issue_count/total_issues/collab_issue_ratio/interaction_frequency/blocker_avg/p50/p95/open_count | 无 | ✅（P2-02 N+1） |
| B6 collaboration/blockers | items[] issue_id/title/blocked_at/resolved_at/response_seconds/status | 无 | ⚠️ **P0-01**（`{items:[...]}` 前端解析错，恒空） |

## Tab3 Git 贡献（G1–G4）

| 接口 | 字段/类型/空值 | source/引导态 | 结果 |
| --- | --- | --- | --- |
| G1 git/eloc | source_status/group_by/total_eloc/human_eloc/agent_eloc/items[]（entity_id 含 "unmapped"、name、eloc、ratio、commit_count、repos） | ready=false → items=[] 各数值 0 | ✅（P3-03 commit_count=PR 数、窗口 merged_at 代理，已文档注明） |
| G2 git/quality | source_status/repo/snapshot_at/coverage/vulnerabilities/duplication_rate；null=无快照 | 无快照→ready=false | ⚠️ P1-03（coverage/duplication 可空扫描风险）；「all」汇总均值口径符合 §3.2 |
| G3 git/repos | source_status/items[] repo/active_commits/active_prs/activity/open_pr_backlog/mtm_p50/mtm_p95 | 未接入→ready=false items=[] | ⚠️ P1-04（active_commits 恒 0、activity=PR 数） |
| G4 git/prs | source_status/items[] pr_number/title/state/author_login/pr_created_at/merged_at/closed_at/additions/deletions/html_url；repo 必填 | 未接入→ready=false items=[] | ✅ |

## Tab4 DORA（D1–D2）

| 接口 | 字段/类型/空值 | source/引导态 | 结果 |
| --- | --- | --- | --- |
| D1 dora/lead-time | source_status/metric/points[] week/p50/p95/sample_count；null=该周无样本 | deploy 未接入→ready=false；merged 依赖 VCS | ✅（P3-02 updated_at 恒 nil） |
| D2 dora/deployments | source_status/total_deployments/failed_deployments/deploy_frequency_weekly/change_failure_rate/mttr_seconds/trend[]/failures[] | 无部署→ready=false 全空 | ⚠️ P1-02（trend[].mttr_seconds 恒 null） |

## 身份与部门（L1–L2）

| 接口 | 字段/类型/空值 | source/引导态 | 结果 |
| --- | --- | --- | --- |
| L1 identity/lifecycle | source_status/onboarding_rate/onboarded_members/new_hires_total/cutoff_events/cutoff_success_count/cutoff_success_rate/recent_events[]（event_type/member_name/occurred_at/result） | 无 identity_import→ready=false | ⚠️ P2-04（result 恒非空，契约允许 null） |
| L2 identity/departments | source_status/items[] department_id/name/member_count/active_members | 无部门→ready=false items=[] | ⚠️ P2-03（name=department_id，无部门名） |

---

## 跨端契约专项（重点拦截项）

| 验收重点 | 结果 |
| --- | --- |
| 契约不一致 | ⚠️ **P0-01 列表接口包裹层**（A3/A5/B3/B6）为唯一跨端硬伤 |
| 空值语义误用 | ✅ 各接口 null/[]/0 后端正确，前端 Schema 默认值合理 |
| 死代码 | ⚠️ P1-01 B3 query 无消费；客户端部分签名带多余参数（P3-05） |
| 性能隐患 | ⚠️ P2-02 blocker N+1；G3/D2 数据未接入属数据源阶段 |
| department_id 未接入错误返回 | ✅ E18：未配置时传值→400（单测覆盖） |
| ELOC 人/机分组 | ✅ 归属与总量双写正确（unmapped 计入 human 总量，接口文档已声明） |
| 秒→天换算 | ✅ 接口统一输出秒，前端 `formatDurationEn/Zh` 统一换算；⚠️ D2 `deploy_frequency_weekly`（次/周）前端在 `tab4-dora.tsx:84` 直接 ×86400 转时长展示，语义错误（P1-05） |

---

## 结论

- 后端 19 个接口契约一致性：**基本通过**（仅 P1-03/P1-02/P1-04/P2 系列需处理）。
- 前端契约一致性：**存在 P0 阻塞**（4 个列表接口解析错误，3 个模块恒空），必须修复后重审。
