# 阶段10 Review 复审报告（修复后复核）—— 研发效能分析平台

> 复审对象：CLO-240 前端修复 commit `8f6da199`、CLO-239 后端修复 commit `a3ed6960`
> 复审基准：上一轮审查结论 `need_modify`（《01-审查报告》《02-修复建议清单》《03-契约一致性校验记录》）
> 复审日期：2026-08-06
> 复审方式：修复 diff 逐项核对 + 后端构建/静态检查/单测复跑 + 前端 typecheck

---

## ① 复审结论

**审查结论：`approve`（通过，放行进入阶段11测试）。**

上一轮 `need_modify` 指出的 **P0 阻塞项（前端 4 个列表接口解析错误）已修复并经复核确认**；P1 级问题（B3 死代码、D2 trend MTTR、G2 可空快照 500、Tab4 部署频率单位换算）全部处理；后端 P2/P3 项除「口径类（需 Leader 确认）」外均已修复并补单测。剩余未改项均为**记录留档的非阻塞事项或须走 PRD §10 Leader 确认的口径项**，不构成进入测试的障碍。

## ② 交付成果清单

| # | 交付物 | 路径/位置 |
| --- | --- | --- |
| 1 | 复审报告（本文件） | `deliverables/engineering-analytics-2026-08-05/审查/04-复审报告-阶段10-修复复核.md` |
| 2 | 前端修复 commit | `8f6da199`（CLO-240，P0-01 / P1-01 / P1-05） |
| 3 | 后端修复 commit | `a3ed6960`（CLO-239，P1-02 / P1-03 / P2-01 / P2-02 / P2-04 / P3-02 + 迁移 255） |
| 4 | 更新后的后端交付文档 | `代码/本地验证报告.md`（20 项单测 + 修复记录）、`代码/接口文档-研发效能分析平台.md` |

**本轮验证记录：**
- 后端：`go build ./...` ✅ / `go vet ./internal/handler/` ✅ / `go test -run "TestAnalytics"`（20 项全绿）✅
- 迁移 255 已应用：`identity_import.result` 已放开 NOT NULL（DB 实查确认）✅
- 前端：`pnpm typecheck`（core / views 全绿）✅

## ③ 修复核对与现存问题说明

### 已修复并复核通过

| 项 | 问题（上轮） | 修复位置 | 复核结果 |
| --- | --- | --- | --- |
| **P0-01** | 前端 A3/A5/B3/B6 用裸数组 Schema 解析后端 `{items:[...]}`/`{points:[...]}`，`parseWithFallback` 恒回退 `[]` → 3 个模块永远空白 | `packages/core/api/client.ts:1867/1892/1943/1990` 先解包 `raw.items`/`raw.points` 再解析 | ✅ 修复正确 |
| P1-01 | B3 `analyticsTopAgentsOptions` query 死代码，Tab2 无 Agent 排行模块 | `queries.ts` 删除该 query；B3 endpoint/client 保留为 API 契约面（PRD §4.2 Tab2 无此模块） | ✅ 删除干净，无残留引用 |
| P1-05 | Tab4 `deploy_frequency_weekly ×86400` 单位错误（次/周当秒转时长） | `tab4-dora.tsx:84` 改为 NumberFlow 展示 `deploy_frequency_weekly` + `次/周` 单位（1 位小数），四语种新增 `deploy_frequency_unit` | ✅ 修复正确 |
| P1-03 | G2 `coverage/duplication_rate` 可空列 `::float8` 扫 NULL → 500 | `analytics.sql` 改选原始可空列，handler `numericToPtr` 空→null、汇总均值跳过 NULL 字段 | ✅ 修复正确（单测 `TestAnalyticsQualityNullableSnapshot` 覆盖） |
| P1-02 | D2 `trend[].mttr_seconds` 恒 null | 新增 `ListAnalyticsDeploymentTrendMttr`（按周 P50 恢复时长），handler 填充 | ✅ 修复正确（单测 `TestAnalyticsDeploymentsTrendMttr`） |
| P2-01 | B1 `merged_count` 不随 department/project 过滤 | `CountAnalyticsMergedIssues` 关联 issue 增加部门/项目过滤 | ✅ 修复正确（单测 `TestAnalyticsMergedCountFiltered`） |
| P2-02 | blocker resolution N+1 | 新增 `ListAnalyticsBlockerResolutions` 批量查询，B6/协作汇总共用 | ✅ 修复正确（单测 `TestAnalyticsBlockerNPlusOne`） |
| P2-04 | L1 `recent_events[].result` 恒非空，契约允许 null | 迁移 255 放开 NOT NULL，handler 透传 | ✅ 修复正确（DB 已应用 + `TestAnalyticsIdentityResultNullable`） |
| P3-02 | D1 deploy / D2 `source_status.updated_at` 恒 nil | 新增 `GetAnalyticsDeploymentLastSync`，取最近部署 `finished_at` | ✅ 修复正确 |

### 未改项（记录留档，不阻塞放行）

| 项 | 说明 | 处置 |
| --- | --- | --- |
| P1-04 | G3 `active_commits` 恒 0：本地 VCS 阶段未接入提交数据 | 二期数据源接入事项，接口契约不受影响，已文档留档 |
| P2-03 | L2 `name`=department_id：无部门名数据源 | 过渡语义，前端按 id 展示，已文档留档 |
| P2-05 | A2 热力图 `issue_volume` 人均口径 | **口径类，须 Leader 确认（PRD §10）** |
| P2-06 | A5 按日覆盖口径与 A4 汇总差异 | **口径类，须 Leader 确认（PRD §10）** |
| P3-01 | `project_id` 不存在性未校验（仅格式校验） | 低风险，已文档留档 |
| P3-03 | G1 `commit_count`=PR 数代理口径 | 已文档注明 |

> 口径类遗留项（P2-05 / P2-06）按 PRD §10 流程交由 Leader 终审确认，不阻塞阶段11 测试；若 Leader 决定调整口径，由对应端在测试阶段前修订并回本审查方备案。

## ④ 下一步协作建议

1. **放行阶段11 测试**（CLO-257）：本轮 `approve`，可将 CLO-257 测试推进至 todo。
2. 测试阶段重点关注此前未接入数据源的引导态链路（Git/DORA/LDAP 未接入时四看板引导态渲染）与部门筛选器（L2 ready 后的 Tab1/Tab2 切片）。
3. 口径类遗留项（P2-05/P2-06）提请 Leader 终审；P1-04（G3 commits）在二期 GitCode 数据源接入后点亮。
4. 阶段12 DevOps（CLO-258）造演示数据时，建议覆盖 `deployment_event`、`repo_quality_snapshot`、`identity_import` 三类表，便于测试四看板真实数据链路。
